# CLAUDE.md — AI 金融风控研究报告项目

## 项目定位

这是一个面向**金融行业资深产品工作者**的 AI 科普与研究型报告页面。读者熟悉风控业务，但不一定熟悉 AI/ML 技术；页面目标是帮助读者建立"产品视角的 AI 认知框架"，而不是培养算法或工程专家。

核心表达重点：

- 用业务语言解释 AI 能力边界，而不是堆技术术语。
- 帮助产品、风控、业务负责人在方案评审、立项决策、供应商评估中问出关键问题。
- 把 ROI、合规风险、实施周期、数据准备、MLOps 成本放在与模型效果同等重要的位置。

## 目标受众

金融公司风控/产品相关人员，典型特征如下：

- 熟悉贷前、贷中、贷后、评分卡、催收、反欺诈等业务逻辑。
- 对 AI/ML 技术理解偏弱，容易被模型名词和算法细节劝退。
- 需要判断 AI 方案是否适合本机构的业务复杂度、合规要求和数据基础。
- 更关注产品决策含义，而不是算法推导过程。

## 当前实现

- 项目是**纯前端单页应用**，无后端、无数据库、无 API 层。
- 页面内容、章节数据、内容渲染器和图表配置主要集中在 `src/App.jsx`。
- 全局入口为 `index.html` -> `src/main.jsx` -> `src/App.jsx`。
- 全局样式在 `src/index.css`，图表容器样式在 `src/App.css`。
- Tailwind 通过 CDN 在 `index.html` 中加载，并在页面内配置扩展色板。

## 技术栈

- React 19 + Vite 8
- Chart.js 4
- JavaScript + JSX，未使用 TypeScript
- Tailwind CSS CDN
- ESLint flat config
- 页面语言：中文

## 常用命令

```bash
npm install
npm run dev
npm run build
npm run lint
npm run preview
```

运行说明：

- `npm run dev` 启动 Vite 开发服务，默认通常为 `localhost:5173`。
- `npm run build` 生成 `dist/` 静态产物。
- `npm run preview` 预览生产构建，默认通常为 `localhost:4173`。
- 如需手动托管 `dist/`，可在 `dist/` 目录运行 `python3 -m http.server 8080`，访问 `localhost:8080`。

## 质量与部署边界

- 当前没有自动化测试框架（无 Vitest/Jest/Playwright）。
- 当前没有 CI、Dockerfile、后端服务或环境变量约定。
- `dist/` 与 `node_modules/` 不应提交。
- 页面运行时依赖 Tailwind CDN；若用于内网、离线环境或严格 CSP 环境，需要改为本地 Tailwind 构建方案。
- 报告中图表数据多为行业参考值或经验化表达，若用于正式对外材料，应补充数据来源、统计口径和引用说明。

## 内容原则

- 每个技术概念优先配一个通俗类比或业务化解释，避免先给定义。
- 关键小节应尽量包含**「产品人视角」**（琥珀色 callout），明确产品人应该追问什么、关注什么风险、如何做决策。
- 技术内容必须服务于产品判断：这项技术适合什么场景、不适合什么场景、落地代价是什么。
- 可视化图表用于支撑论点，而不是展示技术复杂度。
- 金融风控是高风险行业，涉及自主决策、合规、数据隐私、公平性时，要明确风险边界和人工确认节点。

## 内容模型约定

内容由 `chapters` 数组驱动。每个 `chapter` 包含：

- `id`：章节 ID，例如 `ch3`。
- `num`：导航展示用章节编号，例如 `三`。
- `title`：章节标题。
- `sections`：小节列表。

每个 `section` 包含：

- `id`：小节 ID，格式固定为 `ch{n}-s{n}`，例如 `ch4-s2`。
- `title`：小节标题。
- `content`：内容块数组。

支持的内容块类型：

- `p`：段落，字段为 `text`。
- `bullets`：术语型要点列表，字段为 `items: [{ term, desc }]`。
- `list`：普通破折号列表，字段为 `items: string[]`。
- `table`：表格，字段为 `headers` 和 `rows`。
- `callout`：蓝色提示框，用于类比、概念解释、易误区说明，字段为 `text`。
- `product-insight`：琥珀色产品人视角框，字段为 `text`。
- `insight-cards`：卡片网格，主要用于导读和摘要，字段为 `cards: [{ label, text }]`。
- `chart`：Chart.js 图表占位，字段为 `chartId`。

不要随意新增内容块类型；如确需新增，必须同步修改 `ContentBlock` 渲染逻辑。

## ID 与图表约定

- 小节 ID 用于左侧导航跳转和 `IntersectionObserver` 追踪，格式为 `ch{n}-s{n}`。
- 图表 ID 是另一套语义化 key，例如 `model-interpretability`、`data-source-coverage`、`mlops-pipeline`。
- 图表配置统一维护在 `chartConfigs` 对象中。
- `{ type: 'chart', chartId: '...' }` 中的 `chartId` 必须与 `chartConfigs` 的 key 完全一致。
- 新增图表时，需要同时添加内容块和 `chartConfigs` 配置，并确认 canvas id 不重复。

---

## 报告大纲（v2）

### 导读 — 核心结论速览（ch0）

- 6 张卡片：AI 真正价值、选对工具、数据质量、MLOps 成本、LLM 定位、信任渐进路径。

### 一、背景与趋势（ch1）

- 金融风控面临的新挑战：黑产演化、合规压力、实时性、数据碎片化。
- AI 在风控领域的应用演进：规则引擎 -> 传统 ML -> 深度学习 -> LLM/Agent。

### 二、AI 技术基础：从模型到智能体（ch2）

- 2.1 机器学习基础：类比"海量档案总结规律"，覆盖监督/无监督/强化学习，解释树模型主导地位。
- 2.2 大语言模型（LLM）：类比"博学助理"，覆盖 Transformer 演进、核心能力与局限。
- 2.3 AI Agent 体系：类比"配了工具箱的顾问"，覆盖感知/规划/工具/记忆/执行架构。
- 2.4 AI 在金融行业的应用全景：监管态度、可解释性/公平性/隐私/MRM 约束。

### 三、模型：从评分卡到智能体（ch3）

- 3.1 传统信用评分模型：A 卡/B 卡/C 卡、PD/LGD/EAD。
- 3.2 深度学习在风控中的应用：LSTM 时序建模、GNN 欺诈团伙。
- 3.3 大模型与风控结合：规则生成、报告撰写、政策 RAG、非结构化提取。
- 3.4 Agent 化风控：多 Agent 协作编排全流程。
- 3.5 模型可解释性与合规：SHAP、可解释性气泡图。

### 四、数据：特征体系与工程化（ch4）

- 4.1 数据源全景：央行征信、设备指纹、三方多头、行为埋点雷达图。
- 4.2 特征工程方法论：时序窗口、交叉特征、WOE/IV 编码。
- 4.3 自动化特征工程：AutoFE 流水线。
- 4.4 特征稳定性监控：PSI、分布偏移预警。

### 五、场景：全链路 AI 落地（ch5）

- 5.1 贷前：反欺诈与信用评估，含 AI 赋能前后指标对比柱状图。
- 5.2 贷中：动态监控与额度管理。
- 5.3 贷后：智能催收与资产处置。
- 5.4 贯穿全局：策略寻优与 A/B 实验体系。

### 六、交付：MLOps 与工程化闭环（ch6）

- 6.1 模型生命周期管理，含传统模式 vs MLOps 自动化耗时对比横向柱状图。
- 6.2 实时与离线推理架构：Feature Store、200ms 链路。
- 6.3 监控与告警体系：三维度监控、熔断回滚。
- 6.4 从监控到重训的自动闭环。

### 七、挑战与展望（ch7）

- 数据隐私与合规技术：联邦学习、多方安全计算、可信执行环境。
- 模型公平性与算法偏见：Reject Inference 问题。
- 从辅助决策到自主决策的信任鸿沟，含 LLM vs 传统 ML 任务适用性雷达图。
- Agent 在金融高风险场景的可靠性边界。

---

## 页面布局

- 桌面端左侧为固定目录导航，当前章节展开显示小节标题，可点击跳转。
- 移动端顶部展示横向章节导航。
- 右侧为连续滚动内容区，通过 `IntersectionObserver` 追踪当前激活小节。
- 每个小节建议采用：类比/背景 -> 论点段落 -> 要点列表/表格/图表 -> 产品人视角。
- 色调为学术配色：stone/blue 主色，amber 用于产品视角 callout。

## 编辑建议

- 修改章节文案时，优先在对应 `section.id` 附近编辑，不要大范围重排整个 `chapters` 数组。
- 新增章节或小节时，同步检查导航、图表、产品人视角、移动端展示效果。
- 新增图表后运行 `npm run lint` 和 `npm run build`，确认 Chart.js 配置没有运行时错误。
- 若要正式对外发布，应补充 README、数据来源、引用口径、版权声明和免责声明。
